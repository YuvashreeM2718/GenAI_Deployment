# --- Web framework ---
fastapi>=0.118.0,<1
uvicorn[standard]>=0.37.0,<1
# Required by File()/UploadFile in app/routers/documents.py.
# NOT a plain fastapi dependency - it only appears under fastapi's
# "standard"/"all" extras, which this project does not install. Keep it.
python-multipart>=0.0.20,<1
pydantic-settings>=2.11.0,<3
# Required by EmailStr in app/schemas.py.
# NOT a plain pydantic dependency - only under pydantic's "email" extra. Keep it.
email-validator>=2.3.0,<3
 
# --- Database: Amazon RDS for PostgreSQL ---
sqlalchemy[asyncio]>=2.0.43,<3
asyncpg>=0.30.0,<1
alembic>=1.16.5,<2
 
# --- Auth ---
pyjwt>=2.10.1,<3
pwdlib[argon2]>=0.2.1,<1
 
# --- AWS: S3 upload + Bedrock Knowledge Base retrieval ---
boto3>=1.43.64,<2
langchain-aws>=1.7.3,<2
 
# --- LLM: Groq ---
langchain-groq>=1.1.3,<2
 